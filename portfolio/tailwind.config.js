module.exports = {
  darkMode: ["class"],
  content: [
    './pages/**/*.{js,jsx}',
    './components/**/*.{js,jsx}',
    './app/**/*.{js,jsx}',
    './src/**/*.{js,jsx}',
  ],
  prefix: "",
  theme: {
    container: {
      center: true,
      padding: "15px",  // No need to repeat this property twice
    },
    screens: {
      sm: "640px",
      md: "768px",
      lg: "960px",
      xl: "1200px",
    },
    fontFamily: {
      primary: "var(--font-jetbrainsMono)", // This expects a CSS variable rather than a static string
      // The 'accent' key as an object inside fontFamily is invalid syntax
    },
    extend: {
      colors: { 
         primary: "#1B1B1B", // You should define your custom colors in 'extend.colors'
        accent: {  // Move this from fontFamily to extend.colors
          DEFAULT: "#7B61FF",
          hover: "#00e187",
        },
        // Make sure you add this if you use bg-primary
      },
      keyframes: {
        "accordion-down": {
          from: { height: "0" },
          to: { height: "var(--radix-accordion-content-height)" },
        },
        "accordion-up": {
          from: { height: "var(--radix-accordion-content-height)" },
          to: { height: "0" },
        },
      },
      
      animation: {
        "accordion-down": "accordion-down 0.2s ease-out",
        "accordion-up": "accordion-up 0.2s ease-out",
      },
    },
  },
  plugins: [require("tailwindcss-animate")],
};
